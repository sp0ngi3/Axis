services:
  axis-web:
    image: axis-web:development
    build:
      context: ./src/frontend/axis-web
      dockerfile: Dockerfile
      args:
        VITE_API_BASE_URL: http://localhost:8081
    ports:
      - "3001:80"
